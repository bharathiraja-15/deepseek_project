{
  "name": "student-frontend",
  "version": "1.0.0",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    // other dependencies
  },
  "devDependencies": {
    "react-scripts": "^5.0.1",  // Make sure this is here!
    // other dev dependencies
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",  // This uses react-scripts
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
